{
    "name": "Qwen PPO Tuning Dev Container",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },

    // GPU support - pass all GPUs to the container
    // --shm-size=8g is required for NCCL multi-GPU communication
    "runArgs": ["--gpus", "all", "--shm-size=8g"],

    // Features to install
    "features": {
        "ghcr.io/devcontainers/features/common-utils:2": {
            "installZsh": true,
            "configureZshAsDefaultShell": true,
            "installOhMyZsh": true,
            "upgradePackages": true
        },
        "ghcr.io/devcontainers/features/git:1": {
            "version": "latest",
            "ppa": true
        },
        "ghcr.io/devcontainers/features/node:1": {
            "version": "lts"
        }
    },

    // VS Code customizations
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance",
                "ms-toolsai.jupyter",
                "charliermarsh.ruff",
                "tamasfe.even-better-toml",
                "eamodio.gitlens",
                "github.copilot"
            ],
            "settings": {
                "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
                "python.terminal.activateEnvironment": true,
                "[python]": {
                    "editor.defaultFormatter": "charliermarsh.ruff",
                    "editor.formatOnSave": true,
                    "editor.codeActionsOnSave": {
                        "source.fixAll": "explicit",
                        "source.organizeImports": "explicit"
                    }
                }
            }
        }
    },

    // Post-create command to set up the environment
    "postCreateCommand": "bash .devcontainer/post-create.sh",

    // Mount the workspace
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
    "workspaceFolder": "/workspace",

    // Run as non-root user
    "remoteUser": "vscode",

    // Environment variables
    "containerEnv": {
        "PYTHONUNBUFFERED": "1",
        "PYTHONDONTWRITEBYTECODE": "1"
    }
}
